{
  "name": "SmartShopDesktop",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "live-server --port=3000"
  },
  "private": true,
  "devDependencies": {
    "live-server": "^1.2.2",
    "sass": "^1.87.0"
  }
}
